language: deno
fprocess: deno run --allow-net --allow-read mod.ts
welcome_message: |
  You have created a new function which uses Deno.js 1.5.2 and the OpenFaaS
  of-watchdog which gives greater control over HTTP responses.
